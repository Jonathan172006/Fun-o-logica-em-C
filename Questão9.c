#include <stdio.h>
#include <stdlib.h>

float resultado(float nota[i]) {
    float media, soma, i;

    soma += nota[i];
    soma += media;
}

    int main() {
        int i;
        float nota;

        printf("Digite uma nota: ");
        scanf("%f",&nota);

        


return 0;
}